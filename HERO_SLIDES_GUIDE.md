# 🎨 Руководство по управлению Hero-слайдами

## 📋 Обзор

Система управления Hero-слайдами позволяет администраторам легко добавлять, редактировать и управлять баннерами на главной странице сайта через удобную админ-панель.

## ✨ Возможности

### 🔧 Backend (API)
- ✅ **MongoDB модель** для хранения слайдов с метаданными
- ✅ **CRUD API** для управления слайдами
- ✅ **Загрузка в MinIO** с автоматической конвертацией в WebP
- ✅ **Оптимизация изображений** и создание миниатюр
- ✅ **Настройка позиционирования** изображений
- ✅ **Сортировка слайдов** с drag-and-drop

### 🎨 Frontend
- ✅ **Динамическая загрузка** слайдов из API
- ✅ **Плавные переходы** между слайдами
- ✅ **Адаптивный дизайн** для мобильных устройств
- ✅ **Оптимизация производительности** с прогрессивной загрузкой
- ✅ **Настраиваемое позиционирование** для каждого изображения

### 👨‍💼 Админ-панель
- ✅ **Список всех слайдов** с превью
- ✅ **Создание/редактирование** слайдов
- ✅ **Загрузка изображений** с drag-and-drop
- ✅ **Управление порядком** отображения
- ✅ **Активация/деактивация** слайдов
- ✅ **Мобильные версии** текстов

## 🚀 Быстрый старт

### 1. Миграция существующих слайдов

Для переноса существующих слайдов из статичного файла в БД:

```bash
cd YouBaBa/server
npm run migrate:hero-slides
```

**Внимание:** Если в БД уже есть слайды, используйте:
```bash
FORCE_DELETE=true npm run migrate:hero-slides
```

### 2. Доступ к админ-панели

1. Откройте: `http://localhost:5173/admin/login`
2. Войдите с учетными данными администратора
3. Перейдите в раздел **"Hero-слайды"** в боковом меню

## 📝 Использование админ-панели

### Создание нового слайда

1. Нажмите **"+ Добавить слайд"**
2. Заполните обязательные поля:
   - **Изображение** - загрузите картинку (рекомендуется 1920x1080px)
   - **Хештег** - короткий текст в бейдже (например, "Супер!")
   - **Заголовок** - основной заголовок слайда

3. Опциональные поля:
   - **Текст** - дополнительное описание
   - **Мобильный заголовок/текст** - альтернативный контент для мобильных
   - **Позиционирование** - как отображать изображение
   - **Порядок** - число для сортировки (меньше = раньше)
   - **Активен** - показывать ли на сайте

4. Нажмите **"Создать слайд"**

### Редактирование слайда

1. На странице списка слайдов нажмите **"Редактировать"**
2. Измените нужные поля
3. Опционально загрузите новое изображение
4. Нажмите **"Сохранить изменения"**

### Изменение порядка слайдов

На странице списка используйте кнопки **▲** и **▼** для перемещения слайдов вверх/вниз.

### Удаление слайда

1. Нажмите **"Удалить"** на нужном слайде
2. Подтвердите действие, нажав **"Подтвердить удаление"**

**Внимание:** Удаление необратимо и также удалит изображения из хранилища!

## 🎨 Настройка позиционирования изображений

### Background Position
Определяет, какая часть изображения будет видна в hero-секции:

- **По центру** - центр изображения (по умолчанию)
- **Сверху** - верхняя часть изображения
- **Снизу** - нижняя часть
- **Слева/Справа** - боковые части
- **Комбинации** - например, "Сверху слева"

### Object Position
Используется для превью и миниатюр. Настраивается так же, как Background Position.

### Рекомендации по изображениям

- **Формат**: PNG, JPEG, WebP (автоматически конвертируется в WebP)
- **Размер**: 1920x1080px (16:9) для лучшего качества
- **Вес**: до 10MB (будет оптимизирован)
- **Композиция**: важные элементы размещайте по центру или используйте настройки позиционирования

## 🔧 Техническая структура

### API Endpoints

```
GET    /api/hero-slides           - Получить активные слайды (публичный)
GET    /api/hero-slides/admin     - Получить все слайды (админ)
GET    /api/hero-slides/:id       - Получить слайд по ID (админ)
POST   /api/hero-slides           - Создать слайд (админ)
PUT    /api/hero-slides/:id       - Обновить слайд (админ)
DELETE /api/hero-slides/:id       - Удалить слайд (админ)
PATCH  /api/hero-slides/reorder   - Изменить порядок (админ)
```

### Структура модели HeroSlide

```typescript
{
  bgImage: {
    url: string;           // URL в MinIO
    thumbnailUrl: string;  // URL миниатюры
    width: number;
    height: number;
    size: number;
    objectName: string;    // Для удаления
  },
  imagePosition: {
    objectPosition: string;      // CSS object-position
    backgroundPosition: string;  // CSS background-position
  },
  hashtag: string;        // Текст бейджа
  title: string;          // Заголовок
  text: string;           // Описание
  mobileTitle: string;    // Мобильный заголовок (опционально)
  mobileText: string;     // Мобильный текст (опционально)
  order: number;          // Порядок отображения
  isActive: boolean;      // Активность
  createdAt: Date;
  updatedAt: Date;
}
```

### Файлы проекта

**Backend:**
- `server/src/models/HeroSlide.ts` - MongoDB модель
- `server/src/routes/heroSlides.ts` - API роуты
- `server/src/scripts/migrate-hero-slides.ts` - Скрипт миграции

**Frontend:**
- `src/lib/api/heroSlides.ts` - API клиент
- `src/store/heroSlidesStore.ts` - Zustand store
- `src/components/HeroSlider.tsx` - Компонент слайдера
- `src/pages/admin/HeroSlidesPage.tsx` - Список слайдов
- `src/pages/admin/HeroSlideEditPage.tsx` - Создание/редактирование

**Стили:**
- `public/Css/style.css` - Стили hero-слайдера
- `src/styles/admin.css` - Стили админ-панели

## 🐛 Устранение неполадок

### Слайды не отображаются на главной странице

1. Проверьте, что слайды **активны** в админ-панели
2. Откройте консоль браузера и проверьте ошибки
3. Убедитесь, что API запрос `/api/hero-slides` возвращает данные
4. Проверьте соединение с MongoDB и MinIO

### Изображения не загружаются

1. Проверьте настройки MinIO в `.env`:
   ```
   MINIO_ENDPOINT=localhost
   MINIO_PORT=9000
   MINIO_USE_SSL=false
   MINIO_ACCESS_KEY=...
   MINIO_SECRET_KEY=...
   MINIO_BUCKET_NAME=youbaba
   ```

2. Убедитесь, что MinIO запущен и доступен
3. Проверьте логи сервера для ошибок загрузки

### Ошибка при создании/редактировании слайда

1. Проверьте размер файла (макс. 10MB)
2. Убедитесь, что формат изображения поддерживается (PNG, JPEG, WebP)
3. Проверьте права доступа (нужна авторизация)
4. Проверьте логи сервера для деталей ошибки

### Миграция не работает

1. Убедитесь, что изображения находятся в `YouBaBa/public/Images/`
2. Проверьте подключение к MongoDB
3. Проверьте настройки MinIO
4. Используйте `FORCE_DELETE=true` если нужно перезаписать существующие

## 📊 Производительность

### Оптимизации на фронтенде

- ✅ **Прогрессивная загрузка** - текущий и следующий слайд загружаются сразу
- ✅ **Lazy loading** - остальные слайды загружаются в фоне
- ✅ **WebP формат** - уменьшение размера файлов на 30-50%
- ✅ **Плавные переходы** - CSS transitions вместо JS анимаций
- ✅ **Will-change оптимизация** - GPU ускорение

### Оптимизации на бэкенде

- ✅ **Автоматическая конвертация** в WebP при загрузке
- ✅ **Создание миниатюр** для быстрых превью
- ✅ **Индексы MongoDB** для быстрой выборки
- ✅ **Кэширование** на уровне CDN (MinIO)

## 🔐 Безопасность

- ✅ **JWT авторизация** для всех админ эндпоинтов
- ✅ **Валидация типов файлов** при загрузке
- ✅ **Ограничение размера** файлов (10MB)
- ✅ **Санитизация** входных данных
- ✅ **Rate limiting** на API endpoints

## 📚 Дополнительные ресурсы

- [MinIO Documentation](https://min.io/docs/minio/linux/index.html)
- [MongoDB Documentation](https://www.mongodb.com/docs/)
- [Sharp Image Processing](https://sharp.pixelplumbing.com/)

## 💡 Советы

1. **Используйте качественные изображения** - они автоматически оптимизируются
2. **Тестируйте на мобильных** - используйте мобильные версии текстов
3. **Не делайте слишком много слайдов** - оптимально 5-7 штук
4. **Следите за порядком** - первый слайд самый важный
5. **Обновляйте регулярно** - держите контент актуальным

---

**Создано:** 2026-01-30  
**Версия:** 1.0.0  
**Статус:** ✅ Готово к использованию
